# Functional Analysis Notes

Canonical markdown for **Part II — Function Spaces**. This subtree established the layout used by all other `writings/` topics: `book.toml`, `chapters/SUMMARY.md`, numbered `01`–`05` chapters, and **Bridge** sections linking to Part III.

## Build standalone

```bash
cd writings/functional-analysis && mdbook build
```

## Sync into CompMechBook

```bash
./scripts/sync-writings.sh
mdbook build
```
