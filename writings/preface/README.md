# Preface (source)

Canonical markdown for the **Preface**, following the Functional Analysis Notes layout (`book.toml`, `chapters/SUMMARY.md`, numbered chapter files).

Sync into CompMechBook:

```bash
./scripts/sync-writings.sh
```
